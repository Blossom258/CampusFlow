# CampusFlow 项目面试话术（业务细节版）

## 1. 项目开场（30-40 秒）
CampusFlow 是一个学院流程审批系统，我负责前端主导开发和前后端联调。核心链路是「学生提交申请 -> 导师审批 -> 教务办审批 -> 管理员统筹查看」。
技术上用 React + TypeScript + Vite，工程层采用 pnpm Monorepo。我的重点不是做静态页面，而是把权限、接口、性能和可部署性打通。

---

## 2. 路由与权限（对应简历第 1 条）
### 我是怎么落地的
- 用 React Router + `ProtectedRoute` 做两层校验：
  1. 校验登录态（token）；
  2. 校验角色白名单（allowedRoles）。
- 页面权限是按角色拆开的：
  - `student/admin`：`/apply`、`/my-applications`
  - `tutor/academic_office/admin`：`/approval`
  - `admin`：`/workspace` 管理员视图（系统总览、用户角色列表）
- 默认首页跳转 `/workspace`，不同角色进入后看到的是不同内容，不是同一套页面换文案。

### 业务价值
这套权限模型保证了“谁能看什么、谁能做什么”在前端就明确，避免角色串权。

---

## 3. 前后端联调过程（你可以重点讲这一段）
### 3.1 登录与 Token 流程
- 登录接口：`POST /api/auth/login`
- 传参：`username + password`
- 后端返回：`token + role + username + name`
- 我这边把 token 持久化到 `localStorage`（zustand persist），后续请求通过 Axios 请求拦截器自动注入：
  - `Authorization: Bearer <token>`

结论：**Token 是后端签发/返回，前端只负责存储和自动携带。**

### 3.2 申请与审批主链路
我联调时按真实业务顺序走了完整闭环：
1. 学生发起申请：`POST /api/applications`
2. 学生查看我的申请：`GET /api/applications/my`
3. 导师拉取待办：`GET /api/tasks/pending`
4. 导师审批通过：`POST /api/tasks/:id/approve`
5. 教务办再次拉取待办：`GET /api/tasks/pending`

链路结果：申请的 `current_node` 会从“导师审批”流转到“教务办审批”，最终到“审批完成”。

### 3.3 管理员接口
为了体现管理员不是“同页面不同按钮”，我补了管理员专属接口：
- `GET /api/admin/overview`（用户数、申请总量、待办量、处理中/已完成）
- `GET /api/admin/users`（账号与角色列表）

---

## 4. Monorepo 复用（对应简历第 2 条）
### 我是怎么做的
- `apps/*`：业务应用（前端、API）
- `packages/*`：可复用层
  - `workflow-sdk`：流程流转能力
  - `utils`：通用校验/工具
  - `ui-components`：通用容器与展示组件
- 通过 `workspace:*` 方式共享，避免跨模块复制代码。

### 结果
新增业务模块时直接组合已有包，重复开发明显下降，代码边界更清晰。

---

## 5. Axios 二次封装（对应简历第 4 条）
### 我是怎么做的
- 封装统一 `request<T>()`，统一返回结构 `ResponseData<T>`，业务层拿到的数据类型更稳定。
- 请求拦截器自动注入 token（来源是登录接口返回值）。
- 响应拦截器统一处理：
  - 业务错误码（非 200）
  - 网络异常提示
- 这样页面不用重复写 try/catch + 提示逻辑。

### 可讲一句亮点
“我把‘接口规范’沉到 request 层，页面只关心业务数据，降低了联调和维护成本。”

---

## 6. LCP 优化（对应简历第 3 条，详细版）
### 6.1 我先怎么定位 LCP
我用 Lighthouse + Chrome Performance 先做基线分析，登录首屏场景里 LCP 主要落在首屏可视区的大容器（登录 Hero/卡片主内容块）。这类大块元素受 JS 执行、样式就绪、首屏资源体积影响最大。

### 6.2 我做了哪些优化，每项解决什么问题
1. 路由懒加载 + 动态导入
- 把非首屏页面（审批、详情、我的申请等）拆成异步 chunk。
- 效果：减少首屏需要下载和执行的 JS，主线程阻塞更短。

2. Vite 分包
- 将通用依赖和页面代码拆分，降低入口包压力。
- 效果：首包体积下降，首次渲染更快，缓存命中后回访更快。

3. 图片压缩与静态资源优化
- 对大图资源做压缩，避免首屏资源过重。
- 效果：关键资源下载时间缩短，LCP 元素更快完成绘制。

4. 热点路由预加载
- 在菜单 hover 时预加载目标页面 chunk。
- 效果：虽然不直接改变首次 LCP，但显著改善首屏后的切页体感。

### 6.3 最终结果
LCP 从 **3.5s** 降到 **1.6s**。我在面试里会强调：
- 我不是只做“某一个点优化”，而是“先定位瓶颈 -> 分层优化（网络/打包/执行）-> 再回归验证”。

---

## 7. 常见追问回答模板
### Q1：这个项目里你最有价值的贡献是什么？
我会说：把系统从“功能可演示”推进到“角色权限清晰 + 接口链路闭环 + 可上线部署”的状态。

### Q2：你如何证明你做了联调，而不是只改前端？
我会说：我按学生-导师-教务办全链路逐接口验证，并且关注了流转字段变化（如 `current_node`、`status`），不是只看页面按钮是否可点。

### Q3：Axios 的 token 是前端写死的吗？
我会说：不是。token 来自后端 `POST /api/auth/login` 返回，前端只做存储和自动携带。

---

## 8. 收尾（15 秒）
这个项目我做的不只是页面实现，而是把“权限模型、联调闭环、工程复用、性能指标、上线部署”串成了完整交付链路。
